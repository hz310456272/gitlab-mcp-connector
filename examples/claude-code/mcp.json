{
  "_comment": "Add this to your Claude Code MCP settings. Build the repo first, replace server/config paths, and do NOT put real tokens in this file.",
  "mcpServers": {
    "gitlab": {
      "command": "node",
      "args": ["/absolute/path/to/gitlab-mcp-connector/dist/server.js"],
      "env": {
        "GITLAB_MCP_CONFIG": "/absolute/path/to/gitlab-mcp-config.json"
      }
    }
  }
}
