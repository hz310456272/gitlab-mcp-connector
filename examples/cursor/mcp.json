{
  "_comment": "Add this to your Cursor MCP settings. Replace the GITLAB_MCP_CONFIG path with your actual config file location. Do NOT put real tokens in this file.",
  "mcpServers": {
    "gitlab": {
      "command": "npx",
      "args": ["-y", "gitlab-mcp-connector"],
      "env": {
        "GITLAB_MCP_CONFIG": "/absolute/path/to/gitlab-mcp-config.json"
      }
    }
  }
}
